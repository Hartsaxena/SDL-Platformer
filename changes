Added basic files for level editor:
    - No code yet, just "Hello World" program
    - In order to run level editor, use "make run_editor" instead of "make run"
    - Updated tasks.json for vscode and makefile for running level editor